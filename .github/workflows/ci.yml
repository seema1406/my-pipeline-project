name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Check Project Files
        run: ls -la

      - name: Check Dockerfile
        run: cat Dockerfile

      - name: Build Docker Image
        run: docker build -t my-pipeline-app .

      - name: Test
        run: echo "Tests passed"
